# Subtraction

## Prelude

The symbol to represent subtraction is `-`.
The notation is `A - B`, with the subtraction symbol in between the two values.

The left side value gets the value on the right side taken away from it.
So in the example of `3 - 1`, 1 gets taken away from 3, resulting in a value of 2.

## The Task

Your task is to subtract one number from another and print the result.

- Write a `print()` statement.
- Inside the parentheses, insert `9 - 3`.
- Run the program when ready.

> [!IMPORTANT]
> Invisible within Sepal.
>
>     try   sepal_execute
>     when  standard_output     = "6"
>     and   source_code       has "print"
>     and   source_code       has "9 - 3"
>     and   length(source_code) = 1
>     catch lesson_passed       = True
>
> [next lesson]
