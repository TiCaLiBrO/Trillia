# Negative Numbers

## Prelude

Numbers can be positive or negative.
Negative numbers are represented by prepending a `-` symbol to a number.
For example, to turn `5` into negative 5, you write it as `-5`.
There **must** be no space between the `-` and the number itself.

This operation may be called the additive inverse operator, as, by flipping the sign of a value, you get its additive inverse (the number that, when added to the original number, you get 0).

> [!NOTE]
> Only 1 symbol can be prepended.
> You cannot have `--5`.
> This is for readability.

> [!NOTE]
> The `-` symbol is considered a *unary operator*.
> Unary operators always operate on the objects they are attached to before the rest of the expression.
> For example `5 + -4` is implicitly the same as `5 + (-4)`.


## The Task





> [!IMPORTANT]
> Invisible within Sepal.
>
>     code
>
> [next lesson](https://github.com/TiCaLiBrO/Trillia/blob/main/root/learning/tutorials/trillia/basic%20arithmetic/absolute%20value/sepalinfo.md)

