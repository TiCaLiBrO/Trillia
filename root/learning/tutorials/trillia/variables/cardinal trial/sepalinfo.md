# Cardinal Trial

You are now in the final trial of the variables section.

You are given this program that swaps variables ordinally:

    a = t
    t = u
    u = v
    v = w
    w = x
    x = y
    y = z
    z = a

Your task is to rewrite this 8-step code in one line such that every swap can be done cardinally (all 8 steps at once).

Good luck.

> [!IMPORTANT]
> Invisible within Sepal.
>
>     test_count = 100
>     tests = []
>     for test in 1-test_count
>         (tests)append(random_integer(1-1000), random_integer(1-1000), random_integer(1-1000), random_integer(1-1000), random_integer(1-1000), random_integer(1-1000), random_integer(1-1000))
>
>     if length(lines_of_code) = 1
>     then
>         for tests
>             t = tests[test][1]
>             u = tests[test][2]
>             v = tests[test][3]
>             w = tests[test][4]
>             x = tests[test][5]
>             y = tests[test][6]
>             z = tests[test][7]
>             sepal_execute
>             if  t = tests[test][2]
>             and u = tests[test][3]
>             and v = tests[test][4]
>             and w = tests[test][5]
>             and x = tests[test][6]
>             and y = tests[test][7]
>             and z = tests[test][1]
>             then lesson_passed = True
>             else print("Trial Failed. Try again.")
>     else         print("Trial Failed. Try again.")
>
> [\trillia]()

<!--    t, u, v, w, x, y, z = u, v, w, x, y, z, t -->
